import { createStore } from 'vuex'
import tournament from "./modules/tournament";

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    tournament
  }
})
